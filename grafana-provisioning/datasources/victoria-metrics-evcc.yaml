apiVersion: 1

prune: true

datasources:
  - name: VictoriaMetrics-evcc
    type: victoriametrics-metrics-datasource
    access: proxy
    url: http://victoria-metrics:8428
    isDefault: false
    basicAuth: true
    basicAuthUser: ${VM_USER}
    secureJsonData:
      basicAuthPassword: ${VM_PASSWORD}
